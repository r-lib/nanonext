# NNG Library Version

Returns the versions of the 'libnng' and 'libmbedtls' libraries used by
the package.

## Usage

``` r
nng_version()
```

## Value

A character vector of length 2.

## Examples

``` r
nng_version()
#> [1] "1.11.0"         "Mbed TLS 3.6.5"
```
