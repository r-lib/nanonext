% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aio.R
\name{device_aio}
\alias{device_aio}
\title{Device (Async)}
\usage{
device_aio(s1, s2 = s1)
}
\arguments{
\item{s1}{a Socket, in raw mode.}

\item{s2}{a Socket, in raw mode, using the complementary protocol to \code{s1}. If
not supplied, defaults to \code{s1} to create a reflector.}
}
\value{
A 'sendAio' (object of class 'sendAio') (invisibly).
}
\description{
Create an asynchronous device: a zero-copy forwarder that relays raw messages
between two Sockets. This is the building block for brokers and proxies, such
as a publisher/subscriber relay or a pair-to-pair bridge.
}
\details{
A device runs on background threads and relays messages until it is stopped or
an error occurs. The returned 'sendAio' resolves only at that point: its
\verb{$result} is an 'unresolved' logical NA while the device is running, and
otherwise the integer exit code (usually the error that caused it to stop,
such as a Socket being closed).

Both Sockets must be opened in raw mode (see \code{\link[=socket]{socket()}}) and use complementary
protocols, that is the peer protocol of each must be the protocol of the other
(for example two 'pair' Sockets, or a 'pub' and a 'sub' Socket). The same
Socket may be supplied for both arguments to create a reflector.

To stop the device, use \code{\link[=stop_aio]{stop_aio()}}, or close either of the Sockets. To block
and wait for the device to stop, use \code{\link[=call_aio]{call_aio()}}.
}
\examples{
s1 <- socket("pair", listen = "inproc://device1", raw = TRUE)
s2 <- socket("pair", listen = "inproc://device2", raw = TRUE)

dev <- device_aio(s1, s2)
dev$result

stop_aio(dev)
dev$result
close(s1)
close(s2)

}
\seealso{
\code{\link[=send_aio]{send_aio()}} for the structure of the returned 'sendAio'.
}
