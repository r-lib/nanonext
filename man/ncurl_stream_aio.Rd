% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ncurl.R
\name{ncurl_stream_aio}
\alias{ncurl_stream_aio}
\alias{ncurl_stream_recv}
\title{ncurl Stream Async}
\usage{
ncurl_stream_aio(
  url,
  method = NULL,
  headers = NULL,
  data = NULL,
  timeout = NULL,
  tls = NULL,
  buffer = 65536L
)

ncurl_stream_recv(stream, timeout = NULL, cv = NULL)
}
\arguments{
\item{url}{the URL address.}

\item{method}{(optional) the HTTP method as a character string. Defaults to
'GET' if not specified, and could also be 'POST', 'PUT' etc.}

\item{headers}{(optional) a named character vector or named list specifying
the HTTP request headers, for example: \cr
\code{c(Authorization = "Bearer APIKEY", "Content-Type" = "text/plain")} or \cr
\code{list(Authorization = "Bearer APIKEY", "Content-Type" = "text/plain")} \cr
An unnamed vector or list will be ignored.}

\item{data}{(optional) request data to be submitted. Must be a character
string or raw vector, and other objects are ignored. If a character vector,
only the first element is taken. When supplying binary data, the
appropriate 'Content-Type' header should be set to specify the binary
format.}

\item{timeout}{(optional) integer value in milliseconds. For
\code{ncurl_stream_aio()}, this bounds opening the request through receipt of the
response head. For \code{ncurl_stream_recv()}, it bounds that receive.}

\item{tls}{(optional) applicable to secure HTTPS sites only, a client TLS
Configuration object created by \code{\link[=tls_config]{tls_config()}}. If missing or NULL,
certificates are not validated.}

\item{buffer}{positive integer maximum number of transport bytes read by one
receive operation.}

\item{stream}{an active 'ncurlStream'.}

\item{cv}{(optional) a 'conditionVariable' to signal when the receive is
complete.}
}
\value{
\code{ncurl_stream_aio()} returns an 'ncurlStreamAio' (also a 'recvAio').
Its value is a named list containing \code{status}, \code{headers}, and \code{stream},
where \code{stream} is an 'ncurlStream'. \code{ncurl_stream_recv()} returns a
'recvAio' whose value is a named list containing raw \code{data} and logical
\code{complete}.
}
\description{
Opens an HTTP(S) request asynchronously and resolves when the response head
is available without consuming the response body. Body bytes are read with
\code{\link[=ncurl_stream_recv]{ncurl_stream_recv()}} as they arrive. Chunked transfer coding is decoded by
the client, so each receive returns entity-body bytes only.
}
\section{Lifecycle}{


Only one receive may be active for a stream. A receive resolves when body
data is available, the response body is complete, an error occurs, or its
timeout elapses. A receive error, including a timeout, is terminal for the
stream. Close the stream after completion or error to release the HTTP
connection. Closing a stream cancels an active receive.
}

\section{Signalling}{


Supplying \code{cv} signals the condition variable when the receive resolves,
using the same signalling contract as \code{\link[=recv_aio]{recv_aio()}}.
}

\section{Public Internet HTTPS}{


When making HTTPS requests over the public internet, you should supply a TLS
configuration to validate server certificates. See \code{\link[=tls_config]{tls_config()}} for
details.
}

\examples{
\dontshow{if (interactive()) withAutoprint(\{ # examplesIf}
opening <- ncurl_stream_aio(
  "https://postman-echo.com/server-events/3",
  timeout = 2000L
)
head <- opening[]
if (!is_error_value(head)) {
  chunk <- ncurl_stream_recv(head$stream, timeout = 2000L)[]
  close(head$stream)
}
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=ncurl_aio]{ncurl_aio()}} for asynchronous transactions that collect the full
response body.
}
